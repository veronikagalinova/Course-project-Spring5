import { Stops.FilterPipe } from './stops.filter.pipe';

describe('Stops.FilterPipe', () => {
  it('create an instance', () => {
    const pipe = new Stops.FilterPipe();
    expect(pipe).toBeTruthy();
  });
});
